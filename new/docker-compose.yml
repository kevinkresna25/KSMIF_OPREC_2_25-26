services:
  oprec-php:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: php-fpm
      args:
        PUID: 1000
        PGID: 1000
    container_name: oprec-php
    networks:
      - oprec-net
    volumes:
      - oprec-data:/var/www/html
      - ./public/images:/var/www/html/public/images
      - ./storage/app/public:/var/www/html/storage/app/public
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - RUN_MIGRATIONS=true
    healthcheck:
      test: ["CMD", "php-fpm", "-t"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: always

  oprec-nginx:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: nginx
    container_name: oprec-nginx
    ports:
      - "127.0.0.1:8080:80"
    networks:
      - oprec-net
    volumes:
      - oprec-data:/var/www/html:ro
      - ./public/images:/var/www/html/public/images:ro
      - ./storage/app/public:/var/www/html/storage/app/public:ro
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      oprec-php:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: always

volumes:
  oprec-data:
    name: oprec-data
    driver: local

networks:
  oprec-net:
    name: oprec-net
    driver: bridge
